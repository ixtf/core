pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id("com.diffplug.spotless") version "6.11.0"
    }
}
rootProject.name = "core"

